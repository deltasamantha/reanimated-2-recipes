import { StyleSheet } from "react-native";
import { colors } from "../../theme/Colors";

export const styles = StyleSheet.create({
    primary: {
        color: colors.textColorPrimary,
    },
    secondary: {
        color: colors.textColorAccent,
    }
});
